# Pastor de Cabras en el Desierto de Sechura

Minijuego hecho con Vite + Vanilla JS y Firebase (Firestore). 

Instrucciones:

1. Instalar dependencias:

```bash
npm install
```

2. Ejecutar en desarrollo:

```bash
npm run dev
```

3. Configurar `src/firebase.js` con tu `firebaseConfig`.

